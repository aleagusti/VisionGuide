# /app

Will contain the official mobile/desktop application (e.g., React Native/Flutter).

- `README.md`: build instructions (to be defined)
- `ui-wireframes/`: wireframes and flows
- `auth/`: account/subscription handling (when backend is available)